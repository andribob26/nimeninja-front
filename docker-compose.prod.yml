services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
    env_file:
      - .env
    restart: unless-stopped
